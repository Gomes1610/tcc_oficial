import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        height: '21%',  
    },
    text: {
        fontSize: 16,
        fontWeight: 'bold',
        paddingLeft: 5,
    },
});

export default styles