import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        //paddingBottom: 5,
    },
    text: {
        fontSize: 16,
        fontWeight: 'bold',
        paddingLeft: 5,
    },
});

export default styles