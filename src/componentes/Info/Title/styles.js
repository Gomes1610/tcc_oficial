import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        height: 100,
        width: '100%',
        justifyContent: 'center',
        alignItems: 'center',
        //borderRadius: 25,
        //backgroundColor: 'white',
        // borderBottomWidth: 1,
        // borderBottomColor: '#3e92a1',
    },
    font: {
        fontSize: 20,
        fontWeight: 'bold',
    },
});

export default styles